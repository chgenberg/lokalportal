# Database (required) — Railway provides this automatically when you add a Postgres plugin
DATABASE_URL="postgresql://user:password@localhost:5432/dbname"

# NextAuth (required in production)
NEXTAUTH_SECRET="generate-a-secure-random-string"
NEXTAUTH_URL="https://hittayta.se"

# Optional: seed API in production (if set, requests must include this secret)
# SEED_SECRET="optional-seed-secret"

# Optional: public site URL for sitemap, robots, OpenGraph
NEXT_PUBLIC_SITE_URL="https://hittayta.se"

# Set TRUST_PROXY=true when behind Railway or another reverse proxy (for IP-based rate limiting)
# TRUST_PROXY="true"

# OpenAI (required for AI listing generator)
# OPENAI_API_KEY="sk-xxxx"

# Contact form email (Resend). If RESEND_API_KEY is not set, form still returns success but no email is sent.
# RESEND_API_KEY="re_xxxx"
# CONTACT_EMAIL_TO="info@hittayta.se"
# RESEND_FROM_EMAIL="noreply@hittayta.se"
