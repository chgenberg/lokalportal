# Database (required)
DATABASE_URL="postgresql://user:password@localhost:5432/dbname"

# NextAuth (required in production)
NEXTAUTH_SECRET="generate-a-secure-random-string"

# Optional: seed API in production (if set, requests must include this secret)
# SEED_SECRET="optional-seed-secret"

# Optional: public site URL for sitemap, robots, OpenGraph
# NEXT_PUBLIC_SITE_URL="https://hittayta.se"
